import { Photo, ReviewImage } from '@/types/types';
import React from 'react'

interface PhotosBody {
    photos: Photo[];
}

export const PhotosBody = (props: PhotosBody) => {
    return (
        <div>ImagesBody</div>
    )
}


export default PhotosBody;